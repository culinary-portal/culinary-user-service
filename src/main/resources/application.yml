server:
  port: 8080
  servlet:
    session:
      cookie:
        domain: localhost
        http-only: true
        max-age: 600
        name: JSESSIONID
        path: /
        same-site: strict
        secure: false
      timeout: 1m
      tracking-modes: cookie


admin.email: admin@admin.com
default.user.photo.url: https://thumbs.dreamstime.com/b/default-avatar-profile-flat-icon-social-media-user-vect[…]human-image-default-avatar-profile-flat-icon-184330869.jpg"

custom.max.session: 1

frontend.host: "http://localhost:4200"

### JDBC logs
logging:
  level:
    org:
      hibernate: ERROR
      springframework:
        jdbc: TRACE
        security: TRACE




      
